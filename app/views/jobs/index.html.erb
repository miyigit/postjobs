<h1>Главная</h1>
<% @jobs.each do |job| %>
  <div class="job">
    <h2><%= link_to job.title, job.url %></h2>
    
  </div>
<% end %>
<p><%= link_to "Разместить объявление", new_job_path %></p>
<p><%= paginate @jobs %></p>
